<% if @groups %>
  <% @groups.each do |group| %>
    <h3><%= link_to group.name, group_path(group) %></h3>

    <p class="indented-text"><%= group.location %></p>

    <p class="indented-text"><%= group.description %></p>
  <% end %>
<% end %>
