<h3>All Groups</h3>
<hr>
<% if @groups %>
  <% @groups.each do |group| %>
    <div class="well">
      <h3><%= link_to group.name, group_path(group) %></h3>
      <p>
        <%= group.location %>
      </p>
      <p>
        <%= group.description %>
      </p>
    </div>
  <% end %>
<% end %>
