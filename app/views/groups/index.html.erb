<% if @groups %>
  <dl>
    <% @groups.each do |group| %>
      <h2><%= link_to group.name, group_path(group) %></h2>
      <p class="light-grey"><%= group.location %></p>
      <p class="light-grey"><%= group.description %></p>
    <% end %>
  </dl>
<% end %>
