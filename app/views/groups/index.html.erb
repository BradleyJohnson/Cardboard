<% if @groups %>
  <dl>
    <% @groups.each do |group| %>
      <h2><%= link_to group.name, group_path(group) %></h2>
      <p ><%= group.location %></p>
      <p ><%= group.description %></p>
    <% end %>
  </dl>
<% end %>
